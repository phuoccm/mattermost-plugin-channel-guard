# Build outputs
# (./build.sh output: the packaged plugin tarballs)
/dist/
# (cross-compiled server binaries)
/server/dist/

# webapp build output (rebuilt by ./build.sh via esbuild from /webapp/src/)
/webapp/dist/
/webapp/node_modules/
/webapp/package-lock.json

# Go
*.test
*.out
coverage.txt
coverage.html
/vendor/

# Node (only relevant if a webapp bundler is introduced)
/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local env / secrets
.env
.env.local
